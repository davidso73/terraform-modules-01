{
  "AWS_REGION": "{{ aws_region }}",
  "DATABASE_ENDPOINT": "{{ rds_endpoint }}",
  "DATABASE_USER": "{{ db_username }}",
  "DATABASE_PASSWORD": "{{ db_password }}",
  "STORAGE_S3_BUCKET": "{{ s3_bucket_name }}",
  "ALERTS_SNS_ARN": "{{ sns_topic_arn }}",
  "EXECUTION_ENVIRONMENT": "production"
}